apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeinfo-backend.fullname" . }}-test-connection"
  labels:
    {{- include "kubeinfo-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeinfo-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
